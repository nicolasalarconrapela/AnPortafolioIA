# --- SERVER CONFIGURATION ---
PORT=3001
HOST=0.0.0.0

# Public URL of the backend (useful for CORS or redirects)
# EXTERNAL_URL=http://localhost:3001

# --- FIREBASE ADMIN CONFIGURATION ---
# These credentials allow the backend to read/write Firestore and manage Auth users.
# Generate them at: Firebase Console -> Project Settings -> Service Accounts -> Generate New Private Key
# NOTE: Download the JSON file and extract these 3 values.

FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com

# IMPORTANT: Keep the newlines (\n) exactly as they are in the JSON file inside the quotes.
# Example: "-----BEGIN PRIVATE KEY-----\nMIIEvgI...\n-----END PRIVATE KEY-----\n"
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_KEY_HERE\n-----END PRIVATE KEY-----\n"

